# video-pipeline 專案說明

影片製作管線,由四個 skill 串接:
`video-concepting`(創意發想)→ `script-generation`(劇本)→ `visual-asset-generation`(圖像素材)→ `storyboard-generation`(分鏡與影片生成)。

## 自我反省機制(共 4 類)

讓產出更收斂、更對使用者胃口的機制,分成四類:

### 1. 事後修正型 —「不喜歡就走 refine-loop」(全管線共用,核心)

共用流程在 `reference/refine-loop.md`,四步骨架:**診斷病因 → 模糊回饋三段式反問 → 單變因修改(先給使用者看再改)→ 收斂後沉澱**。內含三個子機制:

- **病因診斷表**:先找病因再改。文字類用 1A 表(角色解讀錯 / 引擎錯 / 結構節奏 / 題材錯),生圖類用 1B 表(prompt 沒寫到 / 模型沒執行 / 視覺不可行 / 參數 / 品味)。
- **模糊回饋三段式反問**:使用者只說「怪怪的」時,不准悶頭重跑——攤開當前生成邏輯 → 列 2-4 個可調方向 → 請使用者指認。
- **單變因修改 + 升級規則**:一次只改一個變因;同一改法連 2 次無效就升級病因類別(B→C、引擎→題材)。

### 2. 正向萃取型 —「喜歡就提煉原則」

目前只有 `video-concepting`(第四步之二正向收斂循環)有:分析使用者有感的段子的共同點,提煉成明確創作原則確認後,固化成「系列公式」寫回發想紀錄。

### 3. 呈交前自檢型 —「不過關不呈交」(各 skill 階段獨有)

- `script-generation`:寫完自檢清單(OOC 檢核、@引用齊全、選定段子沒寫丟、無分鏡元素等)。
- `visual-asset-generation`:美術技術四項自檢(比例 / 透視 / 光影 / 肢體結構)+ 成品驗收流程。
- `storyboard-generation`:呈交前連戲檢查五項(節拍 / 首尾幀 / 狀態連戲 / 鏡頭紀律 / 美術協調),ffmpeg 抽幀實看。

### 4. 沉澱回流型 —「同坑只踩一次」

refine-loop 步驟 4:有效改法寫回來源文檔、提煉原則回流上游(創作原則→發想紀錄、生圖教訓→模板檔)、設定層變更同步回流。

## 已知缺口

「正向萃取」目前只在發想階段有;劇本、生圖、分鏡階段只有被動的「不滿意→refine-loop」,沒有對應的「有感→提煉原則」。若要更貼合使用者品味,可考慮把正向收斂循環也複製到下游階段。
